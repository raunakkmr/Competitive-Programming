#include <iostream>
#include <string>

using namespace std;

const int N = 1005;
const int SS = 10005;

int n, s;
string arr[N];
int cnt[SS];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> s;
    for (int i = 0; i < n; i++) cin >> arr[i];

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {

        }
    }
}